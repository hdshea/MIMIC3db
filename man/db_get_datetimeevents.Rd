% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_data.R
\name{db_get_datetimeevents}
\alias{db_get_datetimeevents}
\title{Simple table specific reference function for the DATETIMEEVENTS table data}
\usage{
db_get_datetimeevents(con, ...)
}
\arguments{
\item{con}{A DBIConnection object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}

\item{...}{an optional \code{where} parameter with a character string representing a SQL \code{WHERE} clause.}
}
\value{
a tibble with the results.
}
\description{
Note that some internal processing is performed to reflect general usage patterns
for converting \code{DATE} and \code{TIME} fields to POSIXct date-time format.
}
\examples{
con <- RSQLite::dbConnect(RSQLite::SQLite(), ":memory:")
ex <- tibble::tibble(SUBJECT_ID = c(10006, 10011), VALUE = c("2020-10-10", "2020-10-10"))
RSQLite::dbWriteTable(con, "DATETIMEEVENTS", ex)

td <- db_get_datetimeevents(con, where = "WHERE SUBJECT_ID = 10006")

RSQLite::dbDisconnect(con)
}
