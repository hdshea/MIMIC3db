% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_data.R
\name{db_get_outputevents}
\alias{db_get_outputevents}
\title{Simple table specific reference function for the OUTPUTEVENTS table data}
\usage{
db_get_outputevents(con, ...)
}
\arguments{
\item{con}{A DBIConnection object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}

\item{...}{an optional \code{where} parameter with a character string representing a SQL \code{WHERE} clause.}
}
\value{
a tibble with the results.
}
\description{
Simple table specific reference function for the OUTPUTEVENTS table data
}
\examples{
con <- RSQLite::dbConnect(RSQLite::SQLite(), ":memory:")
ex <- tibble::tibble(SUBJECT_ID = c(10006, 10011))
RSQLite::dbWriteTable(con, "OUTPUTEVENTS", ex)

td <- db_get_outputevents(con, where = "WHERE SUBJECT_ID = 10006")

RSQLite::dbDisconnect(con)
}
